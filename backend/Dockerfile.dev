# バックエンド開発用Dockerfile
FROM node:18-alpine

# 作業ディレクトリの設定
WORKDIR /app

# package.jsonとpackage-lock.jsonをコピー
COPY package*.json ./

# 依存パッケージのインストール
RUN npm install

# アプリケーションのソースコードをコピー
COPY . .

# バックエンドAPIのポートを公開
EXPOSE 8080

# 開発サーバーを起動（nodemonでホットリロード対応）
CMD ["npm", "run", "dev"]
